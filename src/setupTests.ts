// Runs before every test file (configured via `test.setupFiles` in vite.config.ts).
// Adds custom matchers like `toBeInTheDocument()` and auto-cleans the DOM after each test.
import "@testing-library/jest-dom/vitest";
